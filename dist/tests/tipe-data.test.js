describe("Data Type", () => {
    it("should must declare", () => {
        const name = "Anggyar Muhamad Yahya";
        const price = 1000000;
        const isVip = true;
        console.info(name);
        console.info(price);
        console.info(isVip);
    });
});
export {};
