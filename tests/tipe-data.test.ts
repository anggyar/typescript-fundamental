describe("Data Type", () => {
    it("should must declare", () => {
        const name: string = "Anggyar Muhamad Yahya";
        const price: number = 1000000;
        const isVip: boolean = true;

        console.info(name);
        console.info(price);
        console.info(isVip);
    });
});
